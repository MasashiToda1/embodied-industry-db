[project]
name = "embodied-industry-db"
version = "0.1.0"
description = "具身产业库：可核查的公司技术栈与时间线数据库"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]

[tool.uv]
package = false
