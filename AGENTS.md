# AGENTS.md — 具身产业库

入库契约。**两条路径都是一等公民**，同一套 schema、同一套门禁：

| 路径 | 谁在做 | 适合什么 |
|---|---|---|
| **agent 批量** | Cursor agent，开 PR | 有页面可解析的：公众号、招投标公告、论文、官网 |
| **自动盯梢** | `Watch` workflow 每天跑，开成 issue | 有公开 feed 或 API 的源：媒体 RSS、arXiv、HF |
| **issue 随手记** | 开一条 issue，手机上也能填 | **想到就记**，不用启服务、不用开 Codespaces |
| **人工手填** | `make serve` 界面上「手工新建事件」 | 一次录好几条、要同时补轴和金额时 |

这两类信息的比例决定这个库的性格。agent 能抓的都是公开的，人工那部分才是别人复制不走的——**所以人工路径不是兜底，是差异化来源**。

设计文档见 [DESIGN.md](DESIGN.md)，读者视角见 [README.md](README.md)。本文只讲「怎么往这个库里加东西」。

---

## 这个库是什么

记录**哪些公司在用什么技术栈、什么时间做了什么事**的公开数据库。只收可核查的事实并按时间排列。

**不做推断、不打分、不排名。** 谁选对了、谁更可能活下来，由使用数据的人自己判断。你的任务是把事实摆到同一条时间轴上，不是替读者下结论。

收录边界是一条成员规则：**以通用机器人本体为最终载体的技术与商业主体**。人形本体、运控小脑、具身基础模型、数据采集、仿真工具链、关键部件、动作内容、系统集成、场景运营、标准检测都在内；纯工业机械臂、AGV、无人机、扫地机在外。

---

## 四层数据模型

| 层 | 位置 | 你能不能改 |
|---|---|---|
| 身份层 | `registry/orgs/`、`registry/datasets/` | 能，但要判得准 |
| 事实层 | `events/<YYYY>/<MM>/*.yaml` | 能，**只追加，不改不删** |
| 视图层 | `build/` | **不能**，它是编译产物 |
| 词表层 | `vocab/` | 加取值可以，改删取值要做迁移 |

核心约束：**视图页上每条事实都必须能回溯到具体事件**。写不出来源的内容不进库。

---

## 入库七步

### 1. 判来源等级

| tier | 什么算 | 必须有 |
|---|---|---|
| `official` | 主体自己发布（官网、公众号、发布会） | url + retrieved + snapshot |
| `primary` | 一手文件（招标公告、专利、论文、工商登记） | url + retrieved + snapshot |
| `media` | 媒体报道 | url + retrieved + snapshot |
| `aggregator` | 二手聚合（榜单、转载） | url + retrieved + snapshot |
| `first-party` | 本库自行获取，第三方无法核验 | method + retrieved（无 url 与快照） |

`first-party` 的 `method` 走受控词表（`interview` / `customer-review` / `vendor-inquiry` / `field-observation` / `hands-on-test`）。**只记渠道类型，不记个人姓名、不记可反推身份的细节。**

### 2. 存快照

公开来源必须落一份原始页面到 `snapshots/<YYYY>/<MM>/`。链接会死、通稿会改、官网会悄悄改参数——没有快照，两年后整个库就是一堆无法验证的断言。

后台（`make serve`）会自动存；手写事件时自己存，文件名只用 ASCII。

### 3. 定主体

事件的 `orgs` 只放 `registry/orgs/` 里的产业主体。

**判不准就别建条目。** `layers` 必填且必须命中 `vocab/layers.yaml`。空 layers 进不了库，这是刻意的：宁可少一家，不要一堆分不清位置的壳。

建新主体前**先搜一遍别名**。同一家公司常有多个中文写法，已经栽过四次：傅利叶／傅利叶智能、光轮科技／光轮智能、灵初智能／灵巧智能、亮源新创／光原点。漏查会建出重复条目。

**非具身产业的买方不进 registry**，写进事件的 `counterparties` 纯文本字段。景区、工厂、医院、政府部门是长尾且无边界的，做成实体只会塞满没有产业层、也不会有自身事件的空壳。

### 4. 定事件类型与日期

类型见 `vocab/enums.yaml` 的 `event_types`（13 种）。

`date_precision` **必填**（`day` / `month` / `quarter` / `year`）。产业信息大量只能精确到「2025 年下半年」，没有精度字段就会被迫编日期，时间轴从此不可信。

允许把模糊区间钉成具体日期，但**必须留痕**：`date_basis` 写 `estimated` 或 `derived`，并在 `date_as_stated` 里写清原文怎么说的。

### 5. 抽轴取值

轴取值**必须命中词表**（`vocab/axes-*.yaml`），lint 拒绝自由文本。自由文本能存信息但无法横向对比，而可对比性是本库唯一比新闻聚合强的地方。

`vocab/keywords.yaml` 里的关键词只产生**候选**，不是结论：

- 摘要里出现 MuJoCo 可能在相关工作里，不在本文方法里 → **必须看原文片段再定**
- 「deployment on a Unitree G1」说的是用了谁家硬件，不是谁写的 → **归属只看作者或发布方**
- 论文的轴信号弱于产品发布：它说明团队研究过什么，不说明产品里跑什么

### 6. 写摘要

一句事实，不带判断。**lint 会拦推断性措辞**（我认为／预计／有望／可能是因为／大概率／意味着／显然……完整名单见 `vocab/enums.yaml` 的 `inference_phrases`）。

选型理由只以官方表态记录（`type: statement`）：记「谁在什么时候这么说过」，信不信读者自己看。

### 7. 过门禁

```bash
make preflight
```

跑 lint、夹具、解析器测试、编译。**但它不能替代 PR 上的 Actions**——本地环境和 CI 不一定一致，最终以 PR Checks 为准。

---

## 禁止事项

- **不做推断、不打分、不排名**，不写「这说明该公司…」
- **不填估算值**。金额拿不准就整个 `amount` 留空；`scale` 各项允许留空。留空好过写错
- **不改历史事件**。事实层只追加。技术栈历史取值也不删——那是观测收敛的唯一基础
- **不手改 `build/`**。改事实去 `events/`，改身份去 `registry/`
- **不导入其他库的表格或编排**。可以顺着它们的线索回到原始来源自己取证，见 [DESIGN.md](DESIGN.md) 相关工作一节
- **不记个人姓名**与可反推身份的获取细节
- **金额填了必须带币种**。跨国比价没有币种没有意义

---

## 关键命令

```bash
make setup              # 建 venv 装依赖（换机器第一步）
make serve              # 入库后台，http://127.0.0.1:8790
make lint               # 门禁
make lint-strict        # 发布前门禁，事件不足 3 条的主体也算失败
make test               # 夹具：合规必过、违规必拒
make test-ingest        # 三个解析器测试，不打网络
make build              # 编译主体页与数据集页到 build/
make preview ORG=xxx    # 看某主体编译后长什么样
make preflight          # 推送前一条命令跑全部
```

---

## 提交与 PR

### 分支与提交信息

分支名 `agent/<topic>-<4位随机>`。提交信息用中文，一句话说清改了什么，例如：

```
入库：宇树 H2 Plus 发布，补 sim_stack 两条取值
新建主体：亮源新创（foundation / locomotion）
修正：具深报价事件的 amount 单位从元改为万元
```

### PR 必须包含

**摘要** — 加了哪些事件、涉及哪些主体、动了哪些轴。

**验证证据** — 附受影响主体编译后的样子，而不是 YAML：

```bash
make preview ORG=unitree
```

把输出贴进 PR。**这一步是为了在合并前看见后果**：这条事件把时间线和技术栈变成了什么样，比读 YAML 直观得多。若一条事件影响多个主体，每个都附。

**一手来源清单**（如有）— `make lint` 会单独列出 `first-party` 事件，把这段也贴上，让不可核验的部分始终可见。

### 合并条件

`Gate` workflow 全绿。词表自检、真实数据门禁、夹具、解析器测试、编译，五步都过。

---

## 自动盯梢：只发现，不入库

`Watch` workflow 每天北京时间 09:00 扫一遍源，也可以在 Actions 页面手动触发（带 dry-run 选项）。

**它绝不写 `events/`。** 抓到的是候选不是事实，自动写库等于把所有门禁绕过去。产出分两档：

- **命中 registry 主体** → 单开一条 issue，字段按「手工事件」模板预填，`make intake N=xx` 直接可用
- **新闻只命中关键词** → 汇总成一条 digest，多半是还没收录的公司，逐条判
- **论文没命中主体** → 直接丢弃，连 digest 都不进。本库只收产业主体，而 cs.RO 每天几十篇绝大多数是纯高校成果，全放进来候选表立刻变噪音

源清单在 [`scripts/watch-sources.yaml`](scripts/watch-sources.yaml)，改它不用动代码。当前六类源：媒体 RSS、arXiv、Hugging Face（按主体账号）、**GitHub（按主体账号，新仓 + release）**、Exa 语义搜索、上游 Robotics_Notebooks。

**GitHub 比 HF 宽**：除模型外还有 SDK、部署工具、仿真环境、URDF。`LightwheelAI/usd2mjcf` 这种仓直接暴露技术栈（在用 MuJoCo），HF 上看不出来。只看新仓和 release，不看 commit。账号用 `scripts/resolve_github.py` 填，与 `resolve_hf.py` 共用同一套匹配规则。

release 不能走 `orgs/{o}/events` 接口——它被 star 和 push 事件淹掉，宇树 100 条事件只覆盖 3 天。改成取最近推送的仓逐个查 `/releases`，多几次调用但准。63 家一轮约 5 分钟，每天一次没问题。

**Exa 来自 [Agent-Reach](https://github.com/Panniantong/Agent-Reach)。** 那个项目是路由器不是抓取器——每个渠道底下是一个现成工具，它只检查装了没、教 agent 怎么调。评估过它全部 16 个渠道，对本库有增量价值的只有 Exa 搜索（底下是 `mcporter` + Exa MCP，免费无 key）；GitHub 渠道就是 `gh CLI`、RSS 就是 `feedparser`、网页就是 Jina Reader，都不是新能力。Boss直聘 和 LinkedIn 渠道能拿招聘 JD，但要真 Chrome 加登录态，进不了 Actions，只能给人工入库提效。

Exa 补的是 RSS 的盲区：媒体 feed 只有两家，一条「具身智能 融资」搜索首次实测就命中了智身科技 B 轮和地瓜 4 亿美元 C 轮，两条都没出现在 feed 里。

### 噪音控制

第一版 Exa 上线时一轮命中 131 条，全是垃圾的话人就不看了。现在的规则：

- **HF 批量上传聚成一条**：宇树一周推 63 个数据集，报成「批量发布 63 个数据集」一件事；带 `_test_`、时间戳、纯数字 id 的实验日志直接丢
- **同一件事多来源聚成一条 issue**：主体相同、类型相同、日期相差 ≤3 天的合并，其他来源附在补充里——多源正是 `corroboration: multi` 的依据
- **普通词公司名不参与文本匹配**：Humanoid、Figure、Foundation 这类名字出现在任何标题里都不能当归属，只能靠 `accounts` 结构化命中
- **长名优先**：「UBTECH Walker」要认成优必选，不能被别的短词抢走
- **早报串烧进 digest 不进候选**：一条标题提七八家公司的，命中任何一家都没意义
- **搜索结果只留 30 天内**：Exa 会翻出半年前的旧闻
- **每轮最多 15 个 issue**，溢出进 digest：第一次实跑会把两周存量全倒出来（实测 41 件），一天 41 个 issue 没人看
- **大厂必须同时命中机器人关键词**：机器人只是它们一条业务线，第二天实跑就中了一排蚂蚁办公、字节 CEO 讲飞书、英伟达 IMO 金牌、百度智能体、地平线智驾。名单见 `CONGLOMERATES`
- **新闻里主体名必须在标题**：只在正文提一句的是「提到谁」不是「谁的事」——「前小米铁蛋负责人创业」被记到智元头上，就是正文顺带提了智元
- **跟已入库事件比标题相似度**：同一件事换家媒体报，链接不同内容一样，URL 去重挡不住——昨天刚入库的超维融资第二天又报了一遍
- **同标题不同源强制聚合**：英伟达那条雷峰网和 Exa 各给一次，日期差 4 天就开了两个 issue

**Exa 结果不稳定**：同一条查询两次运行相差可以很大（实测 dry-run 10 条、真跑 1 条，且不报错）。它是发现渠道不是权威索引，今天漏的明天可能又出来。每条查询记返回数、空了重试一次。

**GitHub 的 cron 不准时**：官方明说高峰期可延迟数小时，新建的定时任务第一天常不触发。09:00 没跑就手动触发一次，去重状态保证 cron 晚点来了也不会重报。

**账号匹配规则**（`resolve_hf.py` / `resolve_github.py` 共用）踩出来三条：以显示名为准不看 slug（`brainco` 会匹上 `braincode`）；包含匹配只认前缀（`booster` 会嵌进「Rock 'Em Robotics Booster Club」）；**前缀之后的剩余部分必须是公司后缀**（`jushen` 会匹上 `jushenzhidao` 具身智道、`magiclab` 匹上纽约的 `magiclabnyc`、`pudu` 匹上土耳其经销商）。大厂在两个平台上都有一堆组织，不自动填，留人工。**想让某家公司被自动盯上，就去把它 registry 条目的 `accounts.huggingface` 补上**——HF 是精度最高的源，许可、模态、标签全是结构化字段，发布方就是主体本身，不存在归属歧义。

去重状态存在 `.watch-state.json` 并回写仓库（CI 每次都是全新环境，不回写就会天天重报）。另外还会拿已入库事件的 evidence URL 做一道过滤，已经收过的不再报。

### 踩过的坑

**arXiv 限流很紧**，官方要求请求间隔 ≥3 秒，密集调用会被整个挡掉，而且**返回 406 不是 429**——非常容易误判成参数写错。代码里已经单独识别这个码。每天跑一次不会碰到，调试时记得加 sleep。

**以下源试过不行，别再加**：36氪 `/feed` 与 `/feed-newsflash` 返回 HTML 不是 RSS，机器之心 `/rss` 同样，IT桔子 412 反爬。免费新闻 API（NewsAPI / GNews / NewsData）额度小、中文覆盖一般，而且是二手聚合，在本库 tier 里属最低一档——**媒体自己的 RSS 更实在**。

**微信公众号自动不了。** 需要反检测浏览器，GitHub Actions 里跑不现实。这条长期手工。

**上游只盯两件事，不全量接。** Robotics_Notebooks 一天几十个提交全是技术知识 ingest，全量接进来就是噪音，而且我们定过不重建技术知识、只引用。只看：`schema/institutions.json` 的**新增**条目（他策展过的机构，候选主体的好来源；只报增量，全量对比出来一百多条大厂噪音），以及我们 `ref` 留空的轴取值上游是否已有页面可指。

**页面匹配按连字符分词，不用裸子串。** 裸子串会让 `arm` 命中 `armature-modeling`、`harmonic-drive`、`leftarmmotionsolver` 一堆无关页。

## issue 随手记：最低摩擦的入口

**GitHub 就是后台**，不用启服务、不用开 Codespaces，手机上打开仓库就能填。

Issues → New issue → 选「手工事件（agent 抓不到的）」。最少给四项：**谁、什么时候发生、什么事、怎么知道的**。其余留空。

过后把它转成事件：

```bash
make intake N=12              # 先看解析结果和缺什么，不写盘
make intake N=12 WRITE=--write   # 确认无误再写入 events/
```

转换器（`scripts/intake.py`）会替你做几件事，但**不替你猜**：

- 「2025 年下半年」这类模糊表述钉成具体日期时，自动标 `date_basis: estimated` 并把原话留在 `date_as_stated`
- 金额按单位归一化（万元、亿元、万美元都认），**单位不明确一律留空**——留空好过写错一个量级
- 主体按别名双向匹配；匹配不到就填 `TODO` 并**拒绝写盘**，逼你先建条目
- 一手渠道直接落成 `tier: first-party` + `method`，不需要 url 和快照

轴取值它不猜。手工事件不做关键词抽取，按事实自己判。

### 新主体也走 issue

「新主体（registry 里还没有）」模板带产业层勾选框。提之前先在 `registry/orgs/` 搜一遍别名——同一家公司常有多个中文写法，已经栽过四次。

## 人工入库：一次录好几条时

`make serve` 界面上点**「手工新建事件」**，不需要任何来料，字段全靠手填。

**默认 `tier=first-party`**，因为手工记的东西多数是一手获取、第三方核验不了。与其指望人记得去改，不如默认就诚实；确实有公开链接时自己改回 `official` / `primary` / `media` 并补齐 url 和快照。

一手来源要交代 `method`（受控词表）和 `retrieved`：

| method | 典型场景 |
|---|---|
| `interview` | 与从业者聊到的 |
| `customer-review` | 客户回访、使用反馈 |
| `vendor-inquiry` | 以买方身份向厂商询价 |
| `field-observation` | 展会、现场、门店看到的 |
| `hands-on-test` | 自己实测出来的 |

**只记渠道类型，不记个人姓名、不记可反推身份的获取细节。** 这不是形式主义：那些内容一旦公开就收不回来，而它们对判断数据可靠性并无必要。词表里也刻意没有留自由文本的槽位。

`make lint` 每次都会把 `first-party` 事件单独列出来，所以不可核验的部分始终可见、可复核，不会不知不觉堆起来。

人工事件同样受全部门禁约束——推断性措辞、轴取值必须命中词表、日期精度必填，一条都不放宽。

### 人工改动走不走 PR

单条手填直接推 `main` 可以，`Gate` 在 push 上也跑，但那是**事后**发现问题。批量入库、新建多个主体、或者自己也不太确定的判断，走 PR。

## 入库后台

`make serve` 起后台。除上面的手工路径外，三类来料各有解析器，粘进去自动抽字段：

| 来料 | 粘什么 | 自动得到 |
|---|---|---|
| 微信文章 / 官方消息 | 链接或整页 HTML | 标题、公众号、发布时间、正文；公众号名匹配主体；轴候选 |
| 招投标公告 | 公告正文或整页 HTML | 项目、编号、采购人、中标人、金额（含万元与大写换算）、日期 |
| 论文 / arXiv | arXiv 编号、abs 链接或标题加摘要 | 标题、作者、日期、摘要；轴候选 |

碰到 registry 里还没有的主体，界面上可以当场新建，但必须选至少一个产业层。

### 微信直取会撞限流

`httpx` 直取多数能成，但会遇 CAPTCHA 或空正文。两条退路：

1. 浏览器打开文章、**Cmd+U 看源码、全选复制**，整页 HTML 粘进后台（走粘贴路径时要手填原文链接）
2. 量大且反复撞限流时，考虑引入反检测浏览器方案（Camoufox + `wechat-article-for-ai`），代价是约 700MB 依赖

解析器会识别「该内容已被发布者删除」这类提示页并告警——微信返回的是 HTTP 200，不是错误码，别把提示页当文章存进库。

---

## 数据源优先级

| 源 | 价值 | 备注 |
|---|---|---|
| 招投标 | 唯一能直接给出「谁在买、多少钱」 | 中美欧都有公开平台，全球通用 |
| 招聘 JD | 技术栈探针 | 招 Isaac Lab 工程师就不用猜仿真栈；常比发布会早半年 |
| 数采员招聘 | 数据产能探针 | 同时招 50 个采集员＝自建数据工厂而非外包 |
| 训练场招投标 | 同时暴露出钱方与承接方 | 国内特有现象 |
| 官方发布 | 轴信号最直接 | 公众号、官网、发布会 |
| 论文 | 轴取值最密，但信号最弱 | 研究过什么 ≠ 产品里跑什么 |
| 数据集发布 | 规模模态许可全公开 | 发布节奏有信息量：连续发版说明闭环在转 |

---

## 参考

- [DESIGN.md](DESIGN.md) — 完整 schema 设计与设计理由
- [registry/README.md](registry/README.md) — 身份层字段与加主体流程
- [vocab/README.md](vocab/README.md) — 词表约定与 `ref` 解析
