# AGENTS.md — 具身产业库

给 agent 看的入库契约。人是策展人，agent 是维护者；**人只审 PR，不填表单**。

设计文档见 [DESIGN.md](DESIGN.md)，读者视角见 [README.md](README.md)。本文只讲「怎么往这个库里加东西」。

---

## 这个库是什么

记录**哪些公司在用什么技术栈、什么时间做了什么事**的公开数据库。只收可核查的事实并按时间排列。

**不做推断、不打分、不排名。** 谁选对了、谁更可能活下来，由使用数据的人自己判断。你的任务是把事实摆到同一条时间轴上，不是替读者下结论。

收录边界是一条成员规则：**以通用机器人本体为最终载体的技术与商业主体**。人形本体、运控小脑、具身基础模型、数据采集、仿真工具链、关键部件、动作内容、系统集成、场景运营、标准检测都在内；纯工业机械臂、AGV、无人机、扫地机在外。

---

## 四层数据模型

| 层 | 位置 | 你能不能改 |
|---|---|---|
| 身份层 | `registry/orgs/`、`registry/datasets/` | 能，但要判得准 |
| 事实层 | `events/<YYYY>/<MM>/*.yaml` | 能，**只追加，不改不删** |
| 视图层 | `build/` | **不能**，它是编译产物 |
| 词表层 | `vocab/` | 加取值可以，改删取值要做迁移 |

核心约束：**视图页上每条事实都必须能回溯到具体事件**。写不出来源的内容不进库。

---

## 入库七步

### 1. 判来源等级

| tier | 什么算 | 必须有 |
|---|---|---|
| `official` | 主体自己发布（官网、公众号、发布会） | url + retrieved + snapshot |
| `primary` | 一手文件（招标公告、专利、论文、工商登记） | url + retrieved + snapshot |
| `media` | 媒体报道 | url + retrieved + snapshot |
| `aggregator` | 二手聚合（榜单、转载） | url + retrieved + snapshot |
| `first-party` | 本库自行获取，第三方无法核验 | method + retrieved（无 url 与快照） |

`first-party` 的 `method` 走受控词表（`interview` / `customer-review` / `vendor-inquiry` / `field-observation` / `hands-on-test`）。**只记渠道类型，不记个人姓名、不记可反推身份的细节。**

### 2. 存快照

公开来源必须落一份原始页面到 `snapshots/<YYYY>/<MM>/`。链接会死、通稿会改、官网会悄悄改参数——没有快照，两年后整个库就是一堆无法验证的断言。

后台（`make serve`）会自动存；手写事件时自己存，文件名只用 ASCII。

### 3. 定主体

事件的 `orgs` 只放 `registry/orgs/` 里的产业主体。

**判不准就别建条目。** `layers` 必填且必须命中 `vocab/layers.yaml`。空 layers 进不了库，这是刻意的：宁可少一家，不要一堆分不清位置的壳。

建新主体前**先搜一遍别名**。同一家公司常有多个中文写法，已经栽过四次：傅利叶／傅利叶智能、光轮科技／光轮智能、灵初智能／灵巧智能、亮源新创／光原点。漏查会建出重复条目。

**非具身产业的买方不进 registry**，写进事件的 `counterparties` 纯文本字段。景区、工厂、医院、政府部门是长尾且无边界的，做成实体只会塞满没有产业层、也不会有自身事件的空壳。

### 4. 定事件类型与日期

类型见 `vocab/enums.yaml` 的 `event_types`（13 种）。

`date_precision` **必填**（`day` / `month` / `quarter` / `year`）。产业信息大量只能精确到「2025 年下半年」，没有精度字段就会被迫编日期，时间轴从此不可信。

允许把模糊区间钉成具体日期，但**必须留痕**：`date_basis` 写 `estimated` 或 `derived`，并在 `date_as_stated` 里写清原文怎么说的。

### 5. 抽轴取值

轴取值**必须命中词表**（`vocab/axes-*.yaml`），lint 拒绝自由文本。自由文本能存信息但无法横向对比，而可对比性是本库唯一比新闻聚合强的地方。

`vocab/keywords.yaml` 里的关键词只产生**候选**，不是结论：

- 摘要里出现 MuJoCo 可能在相关工作里，不在本文方法里 → **必须看原文片段再定**
- 「deployment on a Unitree G1」说的是用了谁家硬件，不是谁写的 → **归属只看作者或发布方**
- 论文的轴信号弱于产品发布：它说明团队研究过什么，不说明产品里跑什么

### 6. 写摘要

一句事实，不带判断。**lint 会拦推断性措辞**（我认为／预计／有望／可能是因为／大概率／意味着／显然……完整名单见 `vocab/enums.yaml` 的 `inference_phrases`）。

选型理由只以官方表态记录（`type: statement`）：记「谁在什么时候这么说过」，信不信读者自己看。

### 7. 过门禁

```bash
make preflight
```

跑 lint、夹具、解析器测试、编译。**但它不能替代 PR 上的 Actions**——本地环境和 CI 不一定一致，最终以 PR Checks 为准。

---

## 禁止事项

- **不做推断、不打分、不排名**，不写「这说明该公司…」
- **不填估算值**。金额拿不准就整个 `amount` 留空；`scale` 各项允许留空。留空好过写错
- **不改历史事件**。事实层只追加。技术栈历史取值也不删——那是观测收敛的唯一基础
- **不手改 `build/`**。改事实去 `events/`，改身份去 `registry/`
- **不导入其他库的表格或编排**。可以顺着它们的线索回到原始来源自己取证，见 [DESIGN.md](DESIGN.md) 相关工作一节
- **不记个人姓名**与可反推身份的获取细节
- **金额填了必须带币种**。跨国比价没有币种没有意义

---

## 关键命令

```bash
make setup              # 建 venv 装依赖（换机器第一步）
make serve              # 入库后台，http://127.0.0.1:8790
make lint               # 门禁
make lint-strict        # 发布前门禁，事件不足 3 条的主体也算失败
make test               # 夹具：合规必过、违规必拒
make test-ingest        # 三个解析器测试，不打网络
make build              # 编译主体页与数据集页到 build/
make preview ORG=xxx    # 看某主体编译后长什么样
make preflight          # 推送前一条命令跑全部
```

---

## 提交与 PR

### 分支与提交信息

分支名 `agent/<topic>-<4位随机>`。提交信息用中文，一句话说清改了什么，例如：

```
入库：宇树 H2 Plus 发布，补 sim_stack 两条取值
新建主体：亮源新创（foundation / locomotion）
修正：具深报价事件的 amount 单位从元改为万元
```

### PR 必须包含

**摘要** — 加了哪些事件、涉及哪些主体、动了哪些轴。

**验证证据** — 附受影响主体编译后的样子，而不是 YAML：

```bash
make preview ORG=unitree
```

把输出贴进 PR。**这一步是为了在合并前看见后果**：这条事件把时间线和技术栈变成了什么样，比读 YAML 直观得多。若一条事件影响多个主体，每个都附。

**一手来源清单**（如有）— `make lint` 会单独列出 `first-party` 事件，把这段也贴上，让不可核验的部分始终可见。

### 合并条件

`Gate` workflow 全绿。词表自检、真实数据门禁、夹具、解析器测试、编译，五步都过。

---

## 入库后台

`make serve` 起后台，三类来料各有解析器，粘进去自动抽字段：

| 来料 | 粘什么 | 自动得到 |
|---|---|---|
| 微信文章 / 官方消息 | 链接或整页 HTML | 标题、公众号、发布时间、正文；公众号名匹配主体；轴候选 |
| 招投标公告 | 公告正文或整页 HTML | 项目、编号、采购人、中标人、金额（含万元与大写换算）、日期 |
| 论文 / arXiv | arXiv 编号、abs 链接或标题加摘要 | 标题、作者、日期、摘要；轴候选 |

碰到 registry 里还没有的主体，界面上可以当场新建，但必须选至少一个产业层。

### 微信直取会撞限流

`httpx` 直取多数能成，但会遇 CAPTCHA 或空正文。两条退路：

1. 浏览器打开文章、**Cmd+U 看源码、全选复制**，整页 HTML 粘进后台（走粘贴路径时要手填原文链接）
2. 量大且反复撞限流时，考虑引入反检测浏览器方案（Camoufox + `wechat-article-for-ai`），代价是约 700MB 依赖

解析器会识别「该内容已被发布者删除」这类提示页并告警——微信返回的是 HTTP 200，不是错误码，别把提示页当文章存进库。

---

## 数据源优先级

| 源 | 价值 | 备注 |
|---|---|---|
| 招投标 | 唯一能直接给出「谁在买、多少钱」 | 中美欧都有公开平台，全球通用 |
| 招聘 JD | 技术栈探针 | 招 Isaac Lab 工程师就不用猜仿真栈；常比发布会早半年 |
| 数采员招聘 | 数据产能探针 | 同时招 50 个采集员＝自建数据工厂而非外包 |
| 训练场招投标 | 同时暴露出钱方与承接方 | 国内特有现象 |
| 官方发布 | 轴信号最直接 | 公众号、官网、发布会 |
| 论文 | 轴取值最密，但信号最弱 | 研究过什么 ≠ 产品里跑什么 |
| 数据集发布 | 规模模态许可全公开 | 发布节奏有信息量：连续发版说明闭环在转 |

---

## 参考

- [DESIGN.md](DESIGN.md) — 完整 schema 设计与设计理由
- [registry/README.md](registry/README.md) — 身份层字段与加主体流程
- [vocab/README.md](vocab/README.md) — 词表约定与 `ref` 解析
