# 具身产业库

记录**哪些公司在用什么技术栈、什么时间做了什么事**的公开数据库。覆盖全球，中文优先。

只收集可核查的事实并按时间排列。**不做推断、不打分、不排名**——谁选对了、谁更可能活下来，由使用数据的人自己判断。

> **当前状态：V0.1，数据集刚起步。** schema、受控词表、门禁与入库工具已可用并有测试覆盖；`events/` 还在积累中。
> 这个库的规矩是每条事实都要有来源链接和页面快照，所以宁可慢，不先堆量。

## 和同类库的区别在形态，不在话题

**技术栈可对比、收敛可观测。** 受控词表把技术与商业选择压成固定轴，历史取值不删，同一条轴上所有主体的取值可按时间铺开——分散度下降就是收敛。产品字段写成自由文本就做不到这件事。

**商业信号。** 招投标、部署交付、公开定价、招聘信号、交付形态、目标场景。这一层同类库普遍不收。

**结构化与可核查。** 事件是机器可查的 YAML 而不是 markdown 表格，带 lint 门禁、编译视图和来源快照。链接会死，快照不会。

主体名录、产品发布、融资这些都收——它们是轴取值的原料。同一条原始来源，别人取「发布了什么」给人读，本库取「透露了哪条轴的取值」给机器查。重叠是应该的。

`registry/` 里的名录**自己维护、不外包**：它是事件挂载的骨架，同类库的许可也不允许导入。更要紧的是别人的名录覆盖不到租赁、集成、场景运营这几层——那批主体不发论文不办发布会，只能靠一手认知补。

## 收录边界

一条成员规则，不是列举：**以通用机器人本体为最终载体的技术与商业主体**。

整机本体、运控与小脑、具身基础模型、数据采集与供给、仿真与工具链、关键部件、动作与节目内容、系统集成、场景运营都在内；纯工业机械臂、AGV、无人机、扫地机在外。

## 怎么读

| 目录 | 内容 |
|---|---|
| `registry/orgs/` | 主体身份：名字、别名、成立年份、总部、产业层 |
| `registry/datasets/` | 数据集身份：规模、模态、许可、开放程度 |
| `events/` | 事实层。每条事件带日期、精度、来源链接、快照、佐证状态 |
| `vocab/` | 受控词表。11 条轴的全部合法取值 |
| `build/` | 编译产物：主体页、数据集页、合并 JSON |

主体页是**算出来的**，不是写出来的。页面上每条事实都标着来源事件 id，可以一路追到原始链接。

## 入库后台

`make serve` 起后台，三类来料各有解析器：

| 来料 | 粘什么 | 自动得到 |
|---|---|---|
| 微信文章 / 官方消息 | 链接或整页 HTML | 标题、公众号、发布时间、正文；**公众号名匹配主体**；轴候选 |
| 招投标公告 | 公告正文或整页 HTML | 项目、编号、采购人、中标人、金额（含万元与大写换算）、日期 |
| 论文 / arXiv | arXiv 编号、abs 链接或标题加摘要 | 标题、作者、日期、摘要；轴候选 |

还有一条**手工新建事件**的路径，不需要任何来料——展会见闻、电话回访、访谈、线下观察这类没有页面可解析的东西走它。默认标成 `tier: first-party`，要交代渠道类型和获取日期。

碰到 registry 里还没有的主体，界面上可以**当场新建**——但必须选至少一个产业层，判不准就先别建。

轴候选是 `vocab/keywords.yaml` 里的关键词命中，**只标 ◆ 不预选**，并且连原文片段一起摆出来。命中「MuJoCo」可能来自相关工作而不是本文方法，所以得看片段再勾。

## 几条不能破的规矩

**公开来源的事件必须有可访问的来源链接、抓取日期和页面快照。** 链接会死、通稿会改、官网会悄悄改参数。没有快照，两年后整个库就是一堆无法验证的断言。

**一手来源单独标成 `tier: first-party`。** 访谈、客户回访、以买方身份询价、现场观察、自行实测这类信息第三方核验不了，所以不混进公开来源里假装可核查——它必须交代渠道类型（受控词表）和获取日期，`make lint` 每次都会把这些事件单独列出来。读者自己决定信多少。

> 渠道字段刻意做成受控词表而不是自由文本：自由文本会诱使人写下获取细节和当事人姓名，那些东西一旦公开就收不回来，而它们对判断数据可靠性并无必要。**只记渠道类型，不记个人、不记可反推身份的细节。**

**技术栈只能用词表里的取值。** 自由文本能存信息但无法横向对比，而可对比性是本库唯一比新闻聚合强的地方。

**技术栈的历史取值不删。** 一家公司从 MuJoCo 换到 Isaac Lab，两条都留着，各带生效时间和来源。把所有主体在同一条轴上的取值按时间铺开，才能看出哪条轴在收敛、哪条还在打。

**正文不许出现推断性措辞。** 「预计」「大概率」「可能是因为」这类词由 lint 拦截。中立不靠自觉，靠机器强制。

**选型理由只以官方表态记录。** 公司为什么选某条路线，几乎没有第三方客观记录，但它们自己会在发布会和技术博客里说。本库只记「谁在什么时候这么说过」。

## 用法

```bash
make setup         # 建 venv 装依赖，换机器后第一步
make lint          # 门禁
make lint-strict   # 发布前门禁，事件不足 3 条的主体也算失败
make build         # 编译主体页与数据集页到 build/
make test          # 合规夹具必须过，违规夹具必须被拒
make test-ingest   # 微信与招投标解析器测试，不打网络
make serve         # 入库后台，默认 http://127.0.0.1:8790
```

需要 Python 3.11+ 和 `uv`。绑定地址可用 `EID_HOST` / `EID_PORT` 覆盖。

## 在浏览器里跑（推荐）

不想在本机装环境，或手上没有长期属于自己的机器，用 **GitHub Codespaces**：

仓库页面 → **Code → Codespaces → Create codespace on main**。环境自动装好（`.devcontainer/` 已配好 uv、依赖与自检），然后在终端里：

```bash
make serve
```

右下角会弹出转发地址，点开就是入库后台。投完料在同一个环境里 `git commit && git push`。

## 换机器

整个库是自包含的，没有写死任何机器相关路径。`.venv/`、`build/`、`drafts/` 都不进 git，可重建。

```bash
git clone <remote> && cd embodied-industry-db
make setup && make lint && make serve
```

**快照进 git**，所以证据跟着数据一起走，换机器不丢。待审草稿不进 git——它们是临时状态，换机器前记得先审完或丢弃。

## 收录深度

全覆盖不等于全深度。主体分三档：

- `stub` — 仅身份信息，事件不足 3 条，不进公开索引
- `draft` — 3 条以上带来源事件
- `complete` — 主要轴有取值且事件覆盖近 12 个月

一个只有三行的条目，只要每行都有日期和来源链接，它就是有用的。写得很长但没出处的才是垃圾。**质量按可核查密度算，不按篇幅算。**

## 相关工作与致谢

本库与下面两个库是互补关系，**单向引用，不复制内容**。

**[ImChong/Robotics_Notebooks](https://github.com/ImChong/Robotics_Notebooks)**（MIT，Copyright 2026 Chong Liu）——4000 页以上的机器人技术知识库。本库技术轴的每个取值都指向它的对应页面，技术定义不在这边重建。本库的 schema 与脚本设计也参考了它的 ingest / lint / index 思路，主体种子的中英文名与别名取自它的 `schema/institutions.json`。

**[RealXiaoze/humanoid-motion-intelligence](https://github.com/RealXiaoze/humanoid-motion-intelligence)**（内容 CC BY-NC-SA 4.0）——公司与产品主表、公开信号时间线、数据集主表、求职信息。本库把它当**线索索引**使用：顺着它标注的原始来源去抓一手页面、自己存快照、按本库 schema 记录。

> ⚠️ 它的内容许可 CC BY-NC-SA 4.0 与本库的 CC BY 4.0 不兼容（NC 禁商用、SA 要求衍生同权），**因此不得把它的表格或编排导入本库**。事实本身不受版权保护，受保护的是选材与编排，所以取证必须回到原始来源。

技术定义在第一个库，产业名录与发布融资时间线在第二个库，本库做技术栈收敛、商业信号和结构化可核查这三件它们没做的事。

## 设计文档

完整 schema 设计见 [DESIGN.md](DESIGN.md)。

## 许可

数据与文档 CC BY 4.0，代码 MIT。引用请注明出处并保留来源链接。
